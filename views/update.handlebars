

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <title>Atualização de Evento</title>
   <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #ffffff;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      background-color: #ffffff;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
      border-radius: 5px;
      text-align: center;
    }

    .navbar {
      background-color: #BADABE;
      color: #ffffff;
      padding: 1em;
    }

    .nav-list {
      list-style: none;
      padding: 0;
    }

    .nav-item {
      display: inline;
      margin-right: 20px;
    }

    .nav-item a {
      text-decoration: none;
      color: #000000;
      font-weight: bold;
    }

    .nav-link .dropdown-toggle {
      color: #000000;
    }

    .nav-item a:hover {
      color: #0a7500;
    }

    .navbar-brand {
      font-family: fantasy;
      color: #025008;
      font-size: xx-large;
      font-style: italic;
    }

    .offcanvas-body {
      background-color: #BADABE;
      padding: 30px;
    }

    .offcanvas-header {
      background-color: #BADABE;
    }

    .event-form-container {

      text-align: center;
      width: 50%;
      height: 700px;
      border-radius: 15px;
      /* Centralize horizontalmente */
      margin: 0 auto;
      /* Centralize verticalmente ajustando o espaçamento superior */
      margin-top: 180px;
      margin-bottom: 100px;
      padding: 30px;
      background: rgba(186, 218, 190, 0.22);
      border-radius: 16px;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
      backdrop-filter: blur(4.5px);
      -webkit-backdrop-filter: blur(4.5px);
      border: 1px solid rgba(186, 218, 190, 0.45);
    }

    .form-group {
      margin-bottom: 15px;
    }

    label {
      font-weight: bold;
      display: block;
    }

    /* Defina uma largura específica para os inputs em computadores */
    input  {
      width: 50%;
      height: 40px;
      border-radius: 10px;
      /* Largura de 20% para computadores */
    }
    textarea {
      width: 50%;
      height: 40px;
      border-radius: 10px;
      /* Largura de 20% para computadores */
    }

    .btn-submit {
      background-color: #1cac28;
      /* Cor azul da Globo Esporte */
      color: #ffffff;
      padding: 10px 20px;
      border: none;
      border-radius: 20px;
      cursor: pointer;
      transition: background-color 0.3s;
      margin: 20px;
    }

    .btn-submit:hover {
      background-color: #034d00;
      /* Cor mais escura ao passar o mouse */
    }

    h1 {
      text-align: center;
      color: #025008;
      margin-top: 22px;
      font-family: sans-serif;

      /* Centralize o título */
    }
   
  </style>
</head>

<body>
    <!-- Seu formulário de atualização de evento aqui -->
    <div class="event-form-container">
        <h1>Atualizar Eventos</h1>
    <form class="form" action="/update" method="POST">
         <label for="eventTime">ID:</label>
        <input type="text" value="{{events.dataValues.id}}" name="eventId">
         <label for="eventTime">Esporte:</label>
        <input type="text" value="{{events.dataValues.sport}}" name="sport">
        <label for="hostTeam">Time da casa:</label>
        <input type="text" value="{{events.dataValues.hostTeam}}" name="hostTeam">
        <label for="visitingTeam">Time visitante:</label>
        <input type="text" value="{{events.dataValues.visitingTeam}}" name="visitingTeam">
        <label for="eventTime">Horário do evento:</label>
        <input type="time" value="{{events.dataValues.eventTime}}" name="eventTime">
        <label for="dateEvent">Data do evento (MM/DD/AAAA):</label>
        <input type="date" value="{{events.dataValues.dateEvent}}" name="dateEvent">
        <label for="eventsPlace">Local do evento:</label>
        <textarea name="eventsPlace">{{events.dataValues.eventsPlace}}</textarea>
        </br>
        <button type="submit" class="btn-submit">Atualizar evento</button>
    </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
    integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous">
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.min.js"
    integrity="sha384-7VPbUDkoPSGFnVtYi0QogXtr74QeVeeIs99Qfg5YCF+TidwNdjvaKZX19NZ/e6oz" crossorigin="anonymous">
  </script>
</body>

</html>